Frontend (React)
----------------

1. Install dependencies:
   cd frontend
   npm install

2. Start the app:
   npm start

By default the frontend expects the backend at http://localhost:5000
You can change it by creating a .env file in frontend/ and setting:
REACT_APP_API_URL=http://localhost:5000
